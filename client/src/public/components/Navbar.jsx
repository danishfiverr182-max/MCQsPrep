/**
 * Navbar (public/components)
 *
 * Re-exports the main Navbar from src/components/user/Navbar.jsx.
 * Future prompts that import from "../../public/components/Navbar"
 * will get the same component.
 */
export { default } from "../../components/user/Navbar";
