{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "devCommand": "cd client && npm run dev",
  "installCommand": "npm install && cd client && npm install",
  "framework": "vite",
  "env": {
    "VITE_API_TARGET": {
      "description": "Backend API URL for production (Railway URL)",
      "example": "https://your-app-prod.railway.app/api"
    },
    "VITE_ADMIN_PATH": {
      "description": "Admin secret path",
      "example": "/admin-x9k2"
    }
  }
}
