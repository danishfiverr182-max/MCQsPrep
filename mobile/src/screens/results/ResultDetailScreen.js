import PlaceholderScreen from "../../components/ui/PlaceholderScreen";

// Placeholder — real result detail (score breakdown, section-by-section
// review, link into McqReviewPage-equivalent) comes in a later prompt.
export default function ResultDetailScreen() {
  return (
    <PlaceholderScreen
      title="ResultDetailScreen"
      subtitle="Placeholder — real result detail comes later."
    />
  );
}
